MusixOne frontend scaffold (placeholders)

Instructions:
1. Unzip into your project root (replace /frontend contents).
2. Install dependencies:
   cd frontend
   npm install
3. Add real Supabase keys to .env (VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY)
4. Run dev server:
   npm run dev
