import { Link } from 'react-router-dom'; export default function ArtistCard({ artist }) { if (!artist) return null; return (<Link to={`/artista/${artist.slug}`}><div className='bg-[#0a0a0a] rounded-xl p-4 hover:bg-[#121212] transition text-center cursor-pointer'><img src={artist.avatar} alt={artist.name} className='w-24 h-24 rounded-full object-cover mx-auto'/><h3 className='mt-3 font-semibold truncate'>{artist.name}</h3><p className='text-sm text-gray-400'>{artist.genre}</p></div></Link>); }